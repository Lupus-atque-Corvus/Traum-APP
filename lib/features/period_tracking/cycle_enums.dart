/// Cervical mucus quality, ordered by increasing fertility.
enum CervicalMucus { dry, sticky, creamy, watery, eggWhite }

/// Logged intercourse event for a day.
enum SexEvent { none, protected, unprotected }
