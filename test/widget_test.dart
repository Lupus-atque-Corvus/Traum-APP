import 'package:flutter_test/flutter_test.dart';

void main() {
  testWidgets('TRAUM app smoke test', (WidgetTester tester) async {
    // Basic smoke test — full app tests require mocked providers
    expect(true, isTrue);
  });
}
